The full project documentation lives at the repo root:

  ../PROJECT_DOCUMENTATION.txt

This copy was retired so it cannot drift from the backend + frontend docs.
Use the root file (and ../README.md) instead.
