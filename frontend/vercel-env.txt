# Vercel Production env vars
# Copy into Vercel → Settings → Environment Variables → Production

# DELETE the old VITE_API_URL variable if it still points to railway.app
VITE_PUBLIC_URL=https://aanandotsav.vercel.app

# vercel.json proxies /api/* to Railway — no direct Railway URL needed in the browser.

# After saving → Deployments → Redeploy (required).
