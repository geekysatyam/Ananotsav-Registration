# Vercel Production — copy into Vercel dashboard (Production environment)

VITE_PUBLIC_URL=https://aanandotsav.vercel.app

# Do NOT set VITE_API_URL in production.
# If it exists in Vercel, DELETE it.
# vercel.json proxies /api/* → Railway.

# After saving → Deployments → Redeploy
